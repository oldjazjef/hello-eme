{
  "private": true,
  "workspaces": [
    "server",
    "frontend",
    "./"
  ],
  "scripts": {
    "postinstall": "yarn run postinstall-client",
    "postinstall-client": "yarn workspace @hello-eme/frontend install && yarn workspace @hello-eme/frontend run build",
    "postinstall-server": "yarn workspace server install"
  },
  "packageManager": "yarn@3.2.1",
  "engines": {
    "node": "16.x",
    "yarn": "3.x"
  },
  "dependencies": {
    "postinstall": "^0.7.4"
  }
}
