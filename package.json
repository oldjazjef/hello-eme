{
  "private": true,
  "workspaces": [
    "server",
    "frontend",
    "packages/*"
  ],
  "scripts": {
    "start": "yarn --cwd backend start",
    "build": "yarn --cwd frontend build"
  },
  "packageManager": "yarn@3.2.1"
}
